#include <bits/stdc++.h>
#define rep(i, m, n) for(int i = m; i < n; i++)
#define INF 2e9
#define all(v) v.begin(), v.end()
using namespace std;
typedef long long ll;

vector<int> A;

int main()
{
  int N, P, Q;
  A.resize(N);
  rep (i, 0, N) {
    cin >> A.at(i);
  }

  rep (i, 0, N) {
    rep (j, i, N) {

    }
  }
}
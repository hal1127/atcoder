#include <bits/stdc++.h>
#define rep(i, m, n) for(int i = m; i < n; i++)
#define INF 2e9
#define all(v) v.begin(), v.end()
using namespace std;
typedef long long ll;

int main()
{
  int N;
  cin >> N;
  int X, Y;
  cin >> X >> Y;
  vector<int> A(N), B(N);
  rep (i, 0, N) {
    cin >> A.at(i) >> B.at(i);
  }
  
}
